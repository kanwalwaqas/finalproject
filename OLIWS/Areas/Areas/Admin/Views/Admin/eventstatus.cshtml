@model OLIWS.Models.work_events

@{
    ViewBag.Title = "eventstatus";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>eventstatus</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>work_events</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.work_eventsID)

        <div class="form-group">

            <div class="col-md-10">
                <input type="hidden" name="campigns" value="@Model.campigns" />
                @Html.ValidationMessageFor(model => model.events, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">

            <div class="col-md-10">
                <input type="hidden" name="campigns" value="@Model.campigns" />
                @Html.ValidationMessageFor(model => model.campigns, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">

            <div class="col-md-10">
                <input type="hidden" name="news" value="@Model.news" />
                @Html.ValidationMessageFor(model => model.news, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">

            <div class="col-md-10">
                <input type="hidden" name="notification" value="@Model.notification" />
                @Html.ValidationMessageFor(model => model.notification, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IsActive, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <div class="checkbox">
                    <select name="IsActive" value="@Model.IsActive">
                        <option>
                            True
                        </option>
                        <option>
                            False
                        </option>
                    </select>
                    @Html.ValidationMessageFor(model => model.IsActive, "", new { @class = "text-danger" })
                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
